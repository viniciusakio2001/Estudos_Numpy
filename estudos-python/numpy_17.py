# %%
import numpy as np

# %%
# 1. Id's de Usuários: 0 a 10000
print(f"O dtype que usuaria é o dtype uint16 que armazena {np.iinfo(np.uint16)}")

# 2. Temperaturas: 
print(f"O dtype que usuaria é o dtype float32 que armazena {np.finfo(np.float32)}")

# 3. Contadores
print(f"O dtype que utilizadores seria o tipo booleano")